% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/kodama.R
\name{kodama_pca}
\alias{kodama_pca}
\alias{KODAMA.pca}
\title{Backend-native float32 PCA}
\usage{
kodama_pca(
  data,
  ncomp = 2L,
  center = TRUE,
  scale = FALSE,
  backend = NULL,
  n.cores = NULL,
  gpu.device = 0L,
  seed = 4L,
  oversample = NULL,
  power = NULL
)

KODAMA.pca(
  data,
  ncomp = 2L,
  center = TRUE,
  scale = FALSE,
  backend = NULL,
  n.cores = NULL,
  gpu.device = 0L,
  seed = 4L,
  oversample = NULL,
  power = NULL
)
}
\arguments{
\item{data}{Numeric matrix with observations in rows.}

\item{ncomp}{Number of principal components.}

\item{center}{Whether to center columns.}

\item{scale}{Whether to scale centered columns to unit sample standard deviation.}

\item{backend}{Execution backend: \code{"cpu"}, \code{"cuda"}, or \code{"metal"}.}

\item{n.cores}{Number of worker threads. \code{NULL} uses
\code{options(n.cores = ...)}, then \code{N_CORES}, and otherwise 1.}

\item{gpu.device}{Accelerator device id.}

\item{seed}{Random seed for the Gaussian subspace sketch.}

\item{oversample}{Optional randomized-SVD oversampling width. \code{NULL} uses the
standalone backend's automatic policy.}

\item{power}{Optional randomized-SVD power count. \code{NULL} uses the standalone
backend's automatic policy.}
}
\value{
A list containing scores, loadings, singular values, explained
variance, preprocessing vectors, backend metadata, and runtime.
}
\description{
Backend-native float32 PCA
}
\examples{
fit <- KODAMA.pca(as.matrix(iris[, 1:4]), ncomp = 2)
dim(fit$scores)
}
