% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/singlecell-wrappers.R
\name{RunKODAMAmatrix}
\alias{RunKODAMAmatrix}
\alias{RunKODAMAmatrix.default}
\alias{RunKODAMAmatrix.SingleCellExperiment}
\alias{RunKODAMAmatrix.SpatialExperiment}
\alias{RunKODAMAmatrix.Seurat}
\alias{RunKODAMAmatrix.giotto}
\title{Run KODAMA optimization on matrices and single-cell containers}
\usage{
RunKODAMAmatrix(object, ...)

\method{RunKODAMAmatrix}{default}(object, graph = NULL, ...)

\method{RunKODAMAmatrix}{SingleCellExperiment}(
  object,
  reduction = "PCA",
  dims = 50L,
  graph.name = "KODAMA",
  reuse.graph = TRUE,
  ...
)

\method{RunKODAMAmatrix}{SpatialExperiment}(
  object,
  reduction = "PCA",
  dims = 50L,
  graph.name = "KODAMA",
  use.spatial = TRUE,
  sample.column = "sample_id",
  reuse.graph = TRUE,
  ...
)

\method{RunKODAMAmatrix}{Seurat}(
  object,
  reduction = "pca",
  dims = 50L,
  use.spatial = TRUE,
  reduction.save = "KODAMA",
  reuse.graph = TRUE,
  ...
)

\method{RunKODAMAmatrix}{giotto}(
  object,
  reduction = "pca",
  dims = 50L,
  use.spatial = TRUE,
  reduction.save = "KODAMA",
  reuse.graph = TRUE,
  ...
)
}
\arguments{
\item{object}{Numeric matrix, \code{SingleCellExperiment}, \code{SpatialExperiment},
Seurat object, Giotto object, or list of Seurat objects.}

\item{...}{Arguments passed to \code{\link[=KODAMA.matrix]{KODAMA.matrix()}}.}

\item{graph}{Optional graph supplied for a matrix input.}

\item{reduction}{Name of the input dimensional reduction.}

\item{dims}{Number of leading dimensions, or an integer vector selecting
dimensions.}

\item{graph.name}{Name under which the reusable graph is stored.}

\item{reuse.graph}{Reuse a graph stored in the object when available.}

\item{use.spatial}{Whether spatial coordinates should constrain a spatial
container analysis.}

\item{sample.column}{Column in \code{colData} identifying slides or samples.}

\item{reduction.save}{Name used to store KODAMA state and the final
visualization.}
}
\value{
A KODAMA result for a matrix input, otherwise the updated container.
}
\description{
Object methods reuse a graph previously stored by \code{\link[=RunKODAMAgraph]{RunKODAMAgraph()}} unless
\code{reuse.graph = FALSE}. The optimization result is stored as KODAMA state;
the actual reduced dimension is created by \code{\link[=RunKODAMAvisualization]{RunKODAMAvisualization()}}.
}
