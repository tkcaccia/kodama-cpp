% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/kodama.R
\name{KODAMA.visualization}
\alias{KODAMA.visualization}
\title{Visualize a matrix or KODAMA graph with UMAP or openTSNE}
\usage{
KODAMA.visualization(
  x,
  method = c("UMAP", "t-SNE", "opentsne"),
  init = NULL,
  raw.data = NULL,
  initialize.from.raw = TRUE,
  k = 30L,
  metric = c("euclidean", "cosine", "inner_product"),
  backend = c("cpu", "cuda", "metal"),
  n.cores = 4L,
  gpu.device = 0L,
  n.epochs = 200L,
  n.iter = 500L,
  perplexity = 30,
  graph.mode = c("fuzzy", "binary"),
  seed = 4L,
  ...
)
}
\arguments{
\item{x}{Input matrix, KODAMA result, or KNN graph list.}

\item{method}{Embedding method.}

\item{init}{Optional two-column initialization matrix.}

\item{raw.data}{Optional raw data used for backend-native PCA
initialization. When \code{x} is a raw matrix, that matrix is used
automatically. A stored initialization from \code{KODAMA.matrix()} is reused
first when its backend matches \code{backend}; \code{raw.data} is used only when a
compatible stored start is unavailable.}

\item{initialize.from.raw}{Whether raw-data PCA initialization is the
default when no explicit \code{init} is supplied.}

\item{k}{Number of graph neighbors used by the embedding.}

\item{metric}{Distance or similarity metric used when \code{x} is a matrix.}

\item{backend}{Execution backend: \code{"cpu"}, \code{"cuda"}, or \code{"metal"} for
UMAP and openTSNE.}

\item{n.cores}{Number of CPU worker threads requested by the wrapper.}

\item{gpu.device}{CUDA device id when \code{backend = "cuda"}.}

\item{n.epochs}{Number of UMAP optimization epochs.}

\item{n.iter}{Number of openTSNE optimization iterations.}

\item{perplexity}{openTSNE perplexity.}

\item{graph.mode}{UMAP graph weighting mode. \code{"fuzzy"} is the default;
\code{"binary"} remains available as an explicit compatibility mode.}

\item{seed}{Integer random seed.}

\item{...}{Additional embedding parameters forwarded to the C++ wrapper.}
}
\value{
A two-column numeric matrix with attributes describing the selected
backend, optimizer, initialization source and backend, runtime, and (for
UMAP) fuzzy-graph edge count and maximum weight. The graph diagnostics are
zero for openTSNE because its sparse probability matrix is not a UMAP
fuzzy graph.
}
\description{
Visualize a matrix or KODAMA graph with UMAP or openTSNE
}
