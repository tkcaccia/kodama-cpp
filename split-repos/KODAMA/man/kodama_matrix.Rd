% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/kodama.R
\name{kodama_matrix}
\alias{kodama_matrix}
\alias{KODAMA.matrix}
\title{Run KODAMA matrix optimization}
\usage{
kodama_matrix(
  data = NULL,
  graph = NULL,
  spatial = NULL,
  samples = NULL,
  W = NULL,
  constrain = NULL,
  fix = NULL,
  M = 100L,
  Tcycle = 20L,
  ncomp = NULL,
  landmarks = 10000L,
  splitting = NULL,
  n.cores = NULL,
  graph.neighbors = NULL,
  knn.k = 30L,
  spatial.resolution = 0.3,
  spatial.graph.mix = FALSE,
  spatial.constraint.mode = c("kmeans", "graph", "auto"),
  spatial.mode = c("standard", "population"),
  metric = "euclidean",
  classifier = c("knn", "pls_lda"),
  backend = NULL,
  seed = 1234L,
  folds = 5L,
  visual.init = TRUE,
  progress = TRUE,
  progress.file = NULL,
  apply.kodama.dissimilarity = TRUE,
  return.graph = FALSE,
  ...
)
}
\arguments{
\item{data}{Optional raw numeric matrix. It is required when \code{graph} is not
supplied. The C++ core stores analysis values as float32.}

\item{graph}{Optional \code{KODAMA.graph} result or a bare list with \code{indices}
and \code{distances}.}

\item{spatial}{Optional numeric matrix of spatial coordinates.}

\item{samples}{Optional sample or slide identifier. With more than one
unique value, spatial coordinate 1 is separated exactly as in the original
KODAMA implementation before spatial graph and constraint construction.}

\item{W}{Optional integer vector.}

\item{constrain}{Optional integer vector of sample constraints.}

\item{fix}{Optional integer vector marking fixed samples.}

\item{M}{Number of independent KODAMA runs.}

\item{Tcycle}{Number of optimization cycles per run.}

\item{ncomp}{Number of PLS components for the PLS-LDA classifier.}

\item{landmarks}{Maximum number of samples optimized directly in each run.}

\item{splitting}{Initial number of label classes used for each run.}

\item{n.cores}{CPU worker count. \code{NULL} uses \code{options(n.cores = ...)}, then
\code{N_CORES}, and otherwise 4. For CUDA or Metal matrix optimization, an
explicit \code{0} enables backend-specific automatic independent-run lanes.}

\item{graph.neighbors}{Number of neighbors retained in the returned graph.}

\item{knn.k}{Number of neighbors used by the KNN classifier.}

\item{spatial.resolution}{Resolution parameter for constrained grouping when
optional coordinate constraints are supplied.}

\item{spatial.graph.mix}{Logical flag passed to the C++ constraint builder.}

\item{spatial.constraint.mode}{Constraint construction mode.}

\item{spatial.mode}{Coordinate treatment. \code{"population"} applies the
classic repeated-coordinate regularization independently in every \code{M}
run before spatial clustering. The default \code{"standard"} is unchanged.}

\item{metric}{Distance or similarity metric.}

\item{classifier}{Either \code{"knn"} or \code{"pls_lda"}.}

\item{backend}{Execution backend: \code{"cpu"}, \code{"cuda"}, or \code{"metal"}.}

\item{seed}{Integer random seed.}

\item{folds}{Number of cross-validation folds used by the classifier.}

\item{visual.init}{Whether the same native matrix call performs one float32
PCA and stores both UMAP and openTSNE initializations for reuse by
\code{KODAMA.visualization}.}

\item{progress}{Whether the C++ core prints run/cycle progress.}

\item{progress.file}{Optional progress-log path. When \code{progress=TRUE}, the
native workers append M/Tcycle checkpoints here. A temporary path is
created and announced when this is \code{NULL}.}

\item{apply.kodama.dissimilarity}{Whether to return the KODAMA-corrected
neighbor graph rather than only the base graph.}

\item{return.graph}{\code{FALSE} omits the result graph, \code{TRUE} materializes R
index and distance matrices, and \code{"handle"} retains one float32 graph in
an external C++ pointer for reuse without two large R matrices.}

\item{...}{Reserved internal controls for reproducibility experiments.}
}
\value{
A list returned by the kodama-cpp core. The full-data graph is built
once before all \code{M} runs. The result contains one \code{knn} graph;
\code{knn_is_kodama_corrected}, \code{graph_storage_bytes}, \code{graph_builds}, and
\code{timing} expose its lifecycle. Per-run landmark-selection times are
retained in \code{landmark_seconds}; their sum, mean, and median are also
reported in \code{timing} so landmark construction is not conflated with the
classifier core.
}
\description{
Run KODAMA matrix optimization
}
\examples{
x <- as.matrix(iris[, 1:4])
fit <- KODAMA.matrix(
  data = x, classifier = "knn", M = 1, Tcycle = 1,
  landmarks = 100, splitting = 10, knn.k = 5, n.cores = 1,
  return.graph = FALSE
)
fit$acc
}
