% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/kodama.R
\name{kodama_matrix_graph}
\alias{kodama_matrix_graph}
\alias{KODAMA.matrix.graph}
\title{KODAMA matrix from a precomputed KNN graph}
\usage{
kodama_matrix_graph(
  indices,
  distances = NULL,
  data = NULL,
  spatial = NULL,
  W = NULL,
  constrain = NULL,
  fix = NULL,
  M = 100L,
  Tcycle = 20L,
  ncomp = 50L,
  landmarks = 10000L,
  splitting = 100L,
  n.cores = 4L,
  graph.neighbors = if (is.list(indices)) ncol(indices$indices) else ncol(indices),
  knn.k = 30L,
  spatial.resolution = 0.4,
  spatial.graph.mix = FALSE,
  spatial.constraint.mode = c("kmeans", "graph", "auto"),
  classifier = c("knn", "pls_lda"),
  backend = c("cpu", "cuda", "metal"),
  graph.feature.mode = "laplacian_self_tuning",
  graph.feature.components = 0L,
  graph.feature.steps = 3L,
  seed = 1234L,
  progress = TRUE,
  apply.kodama.dissimilarity = TRUE
)
}
\arguments{
\item{indices}{Integer matrix of neighbor indices, or a list with \code{indices}
and \code{distances}.}

\item{distances}{Numeric matrix of neighbor distances.}

\item{data}{Optional original data matrix. When supplied, KODAMA uses this
float32 geometry for landmark selection and initial splitting, while
reusing the supplied graph for KNN classification/projection and the final
KODAMA graph. When omitted, a self-tuning graph Laplacian geometry is used.}

\item{spatial}{Optional spatial or external coordinate matrix used only for
constrained grouping when supplied.}

\item{W}{Optional starting labels.}

\item{constrain}{Optional group vector.}

\item{fix}{Optional fixed-label mask.}

\item{M}{Number of independent runs.}

\item{Tcycle}{Number of optimization cycles per run.}

\item{ncomp}{Number of graph features/components used by the PLS-LDA path.}

\item{landmarks}{Maximum landmarks optimized directly.}

\item{splitting}{Initial number of classes.}

\item{n.cores}{CPU workers or CUDA automatic lanes when backend permits.}

\item{graph.neighbors}{Number of neighbors retained in the returned graph.}

\item{knn.k}{Number of neighbors used by the KNN classifier.}

\item{spatial.resolution}{Resolution used when spatial constraints are
derived inside the core.}

\item{spatial.graph.mix}{Whether to combine spatial and feature graphs for
grouping.}

\item{spatial.constraint.mode}{Spatial constraint strategy.}

\item{classifier}{Either \code{"knn"} or \code{"pls_lda"}.}

\item{backend}{Execution backend: \code{"cpu"}, \code{"cuda"}, or \code{"metal"}.}

\item{graph.feature.mode}{Graph-to-feature transform for PLS-LDA and
graph-only initialization. The standard path is
\code{"laplacian_self_tuning"}.}

\item{graph.feature.components}{Feature count; \code{0} uses \code{ncomp}.}

\item{graph.feature.steps}{Power iterations used by graph feature extraction.}

\item{seed}{Integer random seed.}

\item{progress}{Whether the C++ core prints progress.}

\item{apply.kodama.dissimilarity}{Whether to return the KODAMA-corrected
graph.}
}
\description{
KODAMA matrix from a precomputed KNN graph
}
